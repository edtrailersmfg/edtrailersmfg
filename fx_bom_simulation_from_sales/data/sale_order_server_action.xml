<odoo>

    <record id="material_req_sim_action" model="ir.actions.server">
        <field name="name">Simular requerimiento de material</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">action = records.simulate_materials_requirement()</field>
    </record>

</odoo>